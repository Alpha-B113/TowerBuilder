class Main {
    function void main() {
        var boolean isFirstLaunch;
        var Tower tower;
        let isFirstLaunch = true;

        do Text.createMessages();
        while(true){
            if (isFirstLaunch | (Keyboard.keyPressed() = 129)){
                let isFirstLaunch = false;
                do Screen.clearScreen();
                do TowerBuilder.run();
            }
        }
        return;
    }
}
